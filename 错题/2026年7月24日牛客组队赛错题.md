# **再买一瓶**

## 题目描述

一瓶饮料售价 $1$ 元。  
每次小明喝完一瓶饮料，他会以 $\frac{a}{b}$ 的概率获奖。如果获奖，他将得到 $c$ 元；否则，他什么也得不到。  
他得到的钱可以用来购买更多饮料。每瓶饮料的获奖事件相互独立。  
小明一开始有 $n$ 元。求他恰好喝完 $m$ 瓶饮料后花光所有钱的概率。

### **输入格式**

每个测试文件包含多组测试数据。第一行包含测试数据的组数 $T$ ($1 \le T \le 2 \times 10^5$)。  
接下来 $T$ 行，每行包含五个整数 $n, m, c, a, b$ ($1 \le n, m, c \le 2 \times 10^6$，$0 \le a < b < 998244353$)，  
表示小明一开始有 $n$ 元，每次获奖得到 $c$ 元，获奖概率为 $\frac{a}{b}$。你需要求出他恰好喝完 $m$ 瓶饮料的概率。

### **输出格式**

输出 $T$ 行，每行包含一个整数：对应测试数据的答案对 $998244353$ 取模的结果。  
形式化地，设 $M = 998244353$。可以证明，答案可以表示为最简分数 $\frac{p}{q}$，其中 $p$ 和 $q$ 为整数且 $q \not\equiv 0 \pmod M$。输出等于 $p \cdot q^{-1} \bmod M$ 的整数。

#### **样例输入**

```c++
4
1 1 2 1 2
1 3 2 1 2
2 2 3 1 3
1 2 2 1 2
```

#### **样例输出**

```
499122177
873463809
776412275
```

**Note**

- 在第一组测试数据中，恰好喝完 $1$ 瓶的唯一方式是第一瓶没有获奖，其概率为 $\frac{1}{2}$。  
- 在第二组测试数据中，恰好喝完 $3$ 瓶的唯一方式是第一瓶获奖且接下来两瓶都没有获奖，其概率为 $\frac{1}{8}$。  
- 在第四组测试数据中，小明一开始有 $1$ 元，每次获奖得到 $2$ 元，因此他不可能恰好喝完 $2$ 瓶。

## 题解

设中奖概率为 $p=a/b$，未中奖概率为 $q=1-p=(b-a)/b$，以下所有除法都在模 $998244353$ 的意义下进行。

若在恰好喝完 $m$ 瓶时一共中了 $w$ 次奖，则手中剩余的钱为：

$n - m + c \times w$

题目要求此时恰好花光，因此必须有：

$m - n = c \times w$

所以当 $m<n$ 或 $(m-n)$ 不能被 $c$ 整除时，答案为 $0$。否则中奖次数已经唯一确定：
$w = (m-n) / c$

接下来只需统计：在长度为 $m$、恰有 $w$ 个中奖的所有结果序列中，有多少个不会在第 $m$ 瓶之前把钱花光。

把中奖记为 $W$，未中奖记为 $L$。喝完一瓶后，手中可继续购买的饮料数的变化量为：
- $W: c-1$
- $L: -1$

记原序列的前缀和为 $S_t$。喝完前 $t$ 瓶后，剩余饮料数为：
$A_t = n + S_t$

一个序列合法，当且仅当：
- $A_t > 0 \quad (0 \le t < m)$
- $A_m = 0$

由 $c \times w = m-n$ 可知总变化量 $S_m = -n$，故第二个条件自动满足。

将序列反转并把每一项取相反数。新序列的两种步长为：
- $W \rightarrow 1-c$
- $L \rightarrow 1$

它们均不超过 $1$，总和为 $n$。新序列的前缀和 $R_k$ 满足：
$R_k = A_{m-k}$

因此原序列合法，当且仅当新序列的所有非空前缀和均为正。

现在可应用 **Raney 引理**：若整数序列每一项不超过 $1$，总和为正整数 $n$，则它的 $m$ 个循环移位中恰有 $n$ 个移位的所有非空前缀和为正。

长度为 $m$、恰有 $w$ 个中奖位置的序列总数为 $C(m, w)$。对每个序列考虑全部 $m$ 个切分位置，根据 Raney 引理，其中恰有 $n$ 个切分后得到合法序列；反过来，每个线性序列恰被计入 $m$ 次。故合法序列数为：

$\frac{n}{m} \times C(m, w)$

每个合法序列的概率都为 $p^w \times q^{m-w}$，最终答案为：

$n \times \text{inv}(m) \times C(m, w) \times p^w \times q^{m-w}$

预处理 $0 \dots \max(m)$ 的阶乘和逆阶乘，即可在 $O(1)$求组合数。单组数据用快速幂求模逆与两项幂。

- 预处理：$O(\max\_m)$
- 单组：$O(\log MOD)$
- 空间：$O(\max\_m)$

其中 $\max\_m \le 2 \times 10^6 < 998244353$，阶乘均可逆。

## 自我分析

没做过这种题，raney引理第一次听，之前没接触过，然后概率题自己也不太擅长所以不会这道题

# **Not Aqre 2**

## 题目描述

给定一个 $n \times m$ 的网格。你需要在每个格子中填入 $0$、$1$、$2$ 中的一个整数。  
如果任意两个共享一条边的格子填有不同的整数，则称该填法为好的。  
输出好的填法数对 $998244353$ 取模的结果。

### **输入格式**

每个测试文件仅包含一组测试数据。  
唯一一行包含两个整数 $n$ 和 $m$ ($1 \le n < 10$，$1 \le m < 998244353$)。

### **输出格式**

输出一个整数表示答案。

#### **样例输入**

```
2 2
```

#### **样例输出**

```
18
```

**Note**

在样例中，网格有 $2$ 行 $2$ 列。  
第一列有 $6$ 种选法：`01`、`02`、`10`、`12`、`21`、`20`。对于第一列的任意一种选法，第二列恰好有 $3$ 种合法的选法。因此答案为 $6 \cdot 3 = 18$。

## 题解

如果把一整列的颜色作为状态，那么一列内部相邻格子必须不同，所以合法列数是：

$3 \cdot 2^{n-1}$

这个数量最多是 $3 \cdot 2^8 = 768$。
矩阵快速幂的复杂度为 $O(S^3 \log m)$，其中 $S$ 为矩阵边长。如果直接对这些完整颜色状态做矩阵快速幂，$S=768$，总运算量达到 $1\times 10^{10}$ 以上，无法通过此题。

**3. 优化状压**

**关键观察：** 对于下一列来说，当前列的颜色具体名字不重要，真正重要的是当前列内部的“形状”。

考虑一列从上到下的颜色为 $a_1, a_2, \dots, a_n$。
由于相邻两个格子不能相同，所以 $a_i \neq a_{i-1}$。

当 $i \ge 3$ 时，$a_i$ 有且只有两种可能：
1.  $a_i = a_{i-2}$ （和上上行相同）；
2.  $a_i$ 是既不同于 $a_{i-1}$，也不同于 $a_{i-2}$ 的第三种颜色。

因此，只要知道前两个颜色不同，再用一个二进制位记录每个 $i \ge 3$ 属于哪一种情况，就能唯一确定这一列的相对形状。
定义状态的第 $i-2$位表示：

$[a_i = a_{i-2}], \quad 3 \le i \le n$

所以状态数量是：
$2^{n-2}$
其中 $n=2$时只有一个空状态。

**4. 求转移矩阵**

可以分别枚举当前列和下一列的$3 \cdot 2^{n-1}$种情况，并求出当前列的形状 $s$，下一列的形状 $t$。

如果当前列和下一列可以挨着（每一行的数字都不相同），将转移矩阵 $M_{s,t}$ 加一。
每个 $M_{s,t}$的值就是从形状 $s$ 到形状 $t$ 的具体方案数。

**补充说明：**
对于一个固定的形状，前两个颜色可以任选两个不同颜色，共 $3 \cdot 2 = 6$ 种。之后每个位置都由形状唯一决定。因此每种形状恰好对应 6 个具体合法列。
特别地，$n=1$ 需要单独处理。

**5. 矩阵快速幂**

第一列可以选择任意形状。每种形状有 6 个具体列，所以初始向量中每个状态的值都是 6。
设状态数为 $S = 2^{n-2}$。

*   **如果 $m=1$：**
    答案显然是：
    
    $6S = 3 \cdot 2^{n-1}$
    
*   **如果 $m > 1$：**
    需要做 $m-1$次列转移。因此答案为：
    $\sum_{t=0}^{S-1} \sum_{s=0}^{S-1} 6 \cdot (M^{m-1})_{s,t}$
    也就是求出 $M^{m-1} 后$，把矩阵所有元素求和，再乘以 6。

**时间复杂度：**
优化后，最大矩阵边长是 $2^7 = 128$，可以在时间复杂度 $O(S^3 \log m)$ 下通过。

**6. 另一种可行思路 (Berlekamp-Massey)**

本质上方案数也能用一个项数为 $S = 2^{n-2}$ 的线性递推式来表示。
你不需要去手工求解这个线性递推式。如果你意识到了此题是一个项数不多的线性递推式，由于模数是固定的质数，可以先暴力 DP 出前 $2S$ 项（或更多项），然后利用 **Berlekamp-Massey 算法** 进行求解。

**笔记提示：**

*   核心优化思路在于**归一化列状态**（只存相对形状，忽略具体数字），从而将状态数从 768 降至最大 128。
*   BM 算法解法相比于构造矩阵，在实现上往往更加通用且代码量较少（适合比赛中使用）。

```c++
#include<bits/stdc++.h>
#define int long long
#define endl '\n'
using namespace std;

const int MOD = 998244353; // 修改1：模数

int matrixsize = 2;

struct matrix {
    vector<vector<int>> jz;

    matrix() {
        jz.assign(matrixsize, vector<int>(matrixsize, 0));
    }

    void clear() {
        for (int i = 0; i < matrixsize; i++) {
            for (int j = 0; j < matrixsize; j++) {
                jz[i][j] = 0;
            }
        }
    }

    void init() {
        clear();
        for (int i = 0; i < matrixsize; i++) {
            jz[i][i] = 1;
        }
    }

    matrix operator +(const matrix &dx) {
        matrix ls;
        for (int i = 0; i < matrixsize; i++) {
            for (int j = 0; j < matrixsize; j++) {
                ls.jz[i][j] = (jz[i][j] + dx.jz[i][j]) % MOD;
            }
        }
        return ls;
    }

    matrix operator -(const matrix &dx) {
        matrix ls;
        for (int i = 0; i < matrixsize; i++) {
            for (int j = 0; j < matrixsize; j++) {
                ls.jz[i][j] = (jz[i][j] - dx.jz[i][j] + MOD) % MOD;
            }
        }
        return ls;
    }

    matrix operator *(const matrix &dx) {
        matrix ls;
        for (int i = 0; i < matrixsize; i++) {
            for (int j = 0; j < matrixsize; j++) {
                for (int k = 0; k < matrixsize; k++) {
                    ls.jz[i][j] = (ls.jz[i][j] + jz[i][k] * dx.jz[k][j]) % MOD;
                }
            }
        }
        return ls;
    }

    matrix ksm(int mi) {
        matrix ls, di = *this;
        ls.init();
        while (mi) {
            if (mi & 1) {
                ls = ls * di;
            }
            mi >>= 1;
            di = di * di;
        }
        return ls;
    }
};

int qpow(int a, int b) {
    int res = 1;
    while (b) {
        if (b & 1) res = res * a % MOD;
        a = a * a % MOD;
        b >>= 1;
    }
    return res;
}

void solve() {
    int n, m;
    cin >> n >> m;

    // n=1 特判
    if (n == 1) {
        cout << 3 * qpow(2, m - 1) % MOD << endl;
        return;
    }

    // 枚举所有合法列
    int up = 1;
    for (int i = 0; i < n; i++) up *= 3;
    vector<int> zt;
    for (int i = 0; i < up; i++) {
        int dq = i;
        int pre = -1;
        int bj = 1;
        for (int j = 0; j < n; j++) {
            if (pre == dq % 3) {
                bj = 0;
            }
            pre = dq % 3;
            dq /= 3;
        }
        if (bj) {
            zt.push_back(i);
        }
    }

    // 提取每个列的形状
    vector<int> xz(zt.size());
    for (int i = 0; i < zt.size(); i++) {
        vector<int> ls;
        int ss = zt[i];
        for (int j = 0; j < n; j++) {   // 只取前 n 位
            ls.push_back(ss % 3);
            ss /= 3;
        }
        int xx = 0;
        for (int j = 2; j < n; j++) {
            if (ls[j] == ls[j - 2]) {
                xx |= (1 << (j - 2));
            }
        }
        xz[i] = xx;
    }

    // 状态数 = 2^{n-2}
    int S = (n == 2) ? 1 : (1 << (n - 2));
    matrixsize = S;
    matrix dp;  // 此时矩阵大小为 S×S

    // 为每个形状选一个代表列（只需选第一个遇到的）
    vector<int> rep(S, -1);
    for (int i = 0; i < zt.size(); i++) {
        if (rep[xz[i]] == -1) {
            rep[xz[i]] = zt[i];
        }
    }

    // 构建转移矩阵：从代表列出发，能转移到哪些形状
    for (int s = 0; s < S; s++) {
        int a = rep[s];
        for (int j = 0; j < zt.size(); j++) {
            int b = zt[j];
            bool ok = true;
            int ta = a, tb = b;
            for (int k = 0; k < n; k++) {
                if (ta % 3 == tb % 3) {
                    ok = false;
                    break;
                }
                ta /= 3;
                tb /= 3;
            }
            if (ok) {
                dp.jz[s][xz[j]] = (dp.jz[s][xz[j]] + 1) % MOD;
            }
        }
    }

    // 特判 m=1
    if (m == 1) {
        cout << 6LL * S % MOD << endl;
        return;
    }

    auto ans = dp.ksm(m - 1);
    int cnt = 0;
    for (int i = 0; i < S; i++) {
        for (int j = 0; j < S; j++) {
            cnt = (cnt + ans.jz[i][j]) % MOD;
        }
    }
    cnt = cnt * 6 % MOD;
    cout << cnt << endl;
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int _ = 1;
    while (_--) {
        solve();
    }
    return 0;
}
```

## 自我分析

这道题想出来了使用矩阵快速幂去解决, 但是不知道怎么缩小状态，导致使用的700多种状态跑矩阵快速幂，题解只保留了形状，但是有点想不通为什么形状的转移关系可以直接来自于颜色的转移关系，这题看到第一眼就像出来了矩阵快速幂,但是看到状态太多，以为是结论题需要找规律，所以便找了半天的规律，没有往压缩状态的思路上去考虑，然后这道题原本$700*700$的转移方式直接压缩进了在了$100 *100$的转移方程之内，这点还是很难想到

# **矩阵标记**

## **题目描述**

给定一个 $n \times m$ 的数字网格，每个格子中包含一个正整数。  
如果存在两个格子 $(r_1, c_1)$ 和 $(r_2, c_2)$ 满足 $r_1 < r_2$ 且 $c_1 < c_2$，且这两个格子中的数字相等，那么所有满足 $r_1 \le r \le r_2$ 且 $c_1 \le c \le c_2$ 的格子 $(r, c)$ 都会被标记。  
输出整个网格最终的标记情况。

### **输入格式**

每个测试文件仅包含一组测试数据。  
第一行包含两个整数 $n, m$ ($1 \le n, m$，$n \times m \le 10^6$)。  
接下来 $n$ 行，每行包含 $m$ 个整数；其中第 $i$ 行包含 $a_{i,1}, a_{i,2}, \dots, a_{i,m}$ ($1 \le a_{i,j} \le n \times m$)，描述该数字网格。

### **输出格式**

输出 $n$ 行，每行包含一个长度为 $m$、由字符 `0` 和 `1` 组成的字符串。  
第 $i$ 行的第 $j$ 个字符为 `1` 表示格子 $(i, j)$ 最终被标记，为 `0` 表示未被标记。

#### **样例输入**

```
2 6
1 2 3 4 5 6
7 8 9 3 4 10
```

#### **样例输出**

```
001110
001110
```

**Note**

在样例中，第 $1$ 行第 $3$ 列的格子和第 $2$ 行第 $4$ 列的格子都含有数字 $3$，因此它们标记了两行中第 $3$ 列到第 $4$ 列的所有格子。  
第 $1$ 行第 $4$ 列的格子和第 $2$ 行第 $5$ 列的格子都含有数字 $4$，因此它们标记了两行中第 $4$ 列到第 $5$ 列的所有格子。两个矩形的并集即为最终答案。

## 题解

固定一个数值 \(x\)。若两个值为 \(x\) 的格子分别在 $(r_1, c_1)$ 与 $(r_2, c_2)$，且满足：
$r_1 < r_2, \quad c_1 < c_2$
它们会将矩形 $[r_1, r_2] \times [c_1, c_2]$ 标记。

不同数值产生的矩形只需取并集，因此可以**独立处理每一种数值**，最后用二维差分叠加所有矩形。

将 \(x\) 出现过的行按升序记为：
$r_1 < r_2 < \dots < r_k$

在同一行中，我们只需要保留 $x$ 出现位置的**最小列** $mn[i]$ 与**最大列** $mx[i]$ 即可。

考虑第 $r_t$ 行和第 $r_{t+1}$ 行之间的任意一条横向分界线。一个跨过该分界线的合法矩形，其**左上角只能来自分界线上方**，**右下角只能来自分界线下方**。

令：
*   $L[t] = \min(mn[1], mn[2], \dots, mn[t])$  （分界线上方所有 \(x\) 出现位置的最小列）
*   $R[t] = \max(mx[t+1], mx[t+2], \dots, mx[k])$（分界线下方所有 \(x\) 出现位置的最大列）

判定条件与映射

若 $L[t] < R[t]$，说明上下两侧确实能各选一个值为 $x$ 的格子作为合法矩形的两个角。并且，所有跨过这条分界线的矩形在相邻两行 $r_t, r_{t+1}$上的并，恰好可以用一个矩形表示：
*   **行区间**：$[r_t, r_{t+1}]$
*   **列区间**：$[L[t], R[t]]$

**操作步骤：**
因此对每个 $t = 1 \dots k-1$，只要 $L[t] < R[t]$，便向二维差分中加入这个矩形。

正确性说明

*   **充分性（算法标记的格子一定是正确的）：**
    对于算法加入的矩形，$L[t]$ 来自某个不晚于 $r_t$ 的出现位置，$R[t]$  来自某个不早于 $r_{t+1}$的出现位置。两者列号满足 $L[t] < R[t]$，故它确实被一对合法的相等格子所标记，**不会多标**。

*   **必要性（所有正确标记的格子都会被算法标记）：**
    反过来，任取一个由 $(r_a, c_a), (r_b, c_b)$ 标记的合法矩形。枚举这两行之间每对相邻的出现行 $r_t, r_{t+1}$，由于 $L[t] \le c_a < c_b \le R[t]$，算法加入的对应小矩形覆盖该高度带内的 $[c_a, c_b]$。这些高度带的并覆盖了 $[r_a, r_b]$，于是原矩形的每个格子都会被算法标记。

**结论：** 两边相等，算法得到的正是全部标记格。

复杂度分析

*   **时间复杂度：** $O(nm)$
*   **空间复杂度：** $O(nm)$

**笔记提示：**

*   通过忽略行间具体的 `0/1/2` 矩阵内容，将“具体格子”的问题转化为“行极值前缀/后缀”的问题，是处理这类大范围二维标记问题的经典优化。
*   由于使用了二维差分，最后只需 $O(nm)$ 的时间即可还原所有被标记的格子并统计答案。

## 自我分析