# 按位最大化（Bitwise Maximization）

## 题目描述

输入模式: 标准输入
输出模式: 标准输出
时间限制: 1 秒
空间限制: 256 MB

小羊有一个非负整数列表和两个空的多重集合。他需要将列表中的每个整数放入两个多重集合之一。
注意，多重集合可以包含重复的值。
为了给小羊的工作评分，他的领导分别计算两个多重集合的按位异或（XOR）值，并将结果相加得到最终得分。小羊希望最大化得分，你能告诉他最高能得多少分吗？
一个多重集合的按位异或值为这个集合的异或和。空的多重集合的按位异或值视为0。

### 输入格式

每个测试包含多组测试用例。第一行包含测试用例数T（1⩽T ⩽$10^4$）。接下来是每组测试用例的描述。
每组测试用例的第一行包含一个整数n（1⩽n⩽5×$10^5$）——列表的长度。
每组测试用例的第二行包含n个整数$a_1$$a_2$,$a_3$,...,$a_n$（0⩽ai <$2^{30}$）——列表中的元素。
保证所有测试用例的n之和不超过5×$10^5$。

### 输出格式

对于每组测试用例，输出一个整数，表示最大得分。

#### 样例输入:

```c++
4
1
1
3
1 2 3
4
1 1 3 3
4
1 2 2 3
```

#### 样例输出:

```c++
1
6
6
4
```

## 题解

两个集合的异或和的异或和显然是整个数组的异或和。 考虑整个数组的异或和结果，对于这个结果中那些是1的位，无论我们 怎么分集合，一定都是一个集合是0，另一个集合是1，不受影响。 而对于剩下的位，我们应该从高到低让每一位尽可能为1。 这些位形成的msk是我们唯一关心的，所以我们只需考虑数组中的元素 关于msk 取与运算的结果，看这些数能形成的最大异或和如何计算。 这件事可以用异或线性基快速解决。时间复杂度为O(nlogM)。

```c++
#include<bits/stdc++.h>
#define int long long
#define endl '\n'
using namespace std;
const int N = -1;
const int MOD = 1;
const int MAXB = 30;
#define MOD debug;

void solve() {
    int n;
    cin >> n;
    vector<int> a(n);
    int sum = 0;
    for (int i = 0; i < n; ++i) {
        cin >> a[i];
        sum ^= a[i];
    }
    int mask = ((1 << MAXB) - 1) ^ sum;
    vector<int> basis(MAXB, 0);
    auto insert = [&](int x) {
        for (int i = MAXB - 1; i >= 0; --i) {
            if ((x >> i) & 1) {
                if (!basis[i]) {
                    basis[i] = x;
                    break;
                }
                x ^= basis[i];
            }
        }
    };
    for (int v: a) {
        insert(v & mask);
    }
    int mx = 0;
    for (int i = MAXB - 1; i >= 0; --i) {
        if ((mx ^ basis[i]) > mx) {
            mx ^= basis[i];
        }
    }
    int ans = sum + 2 * mx;
    cout << ans << endl;
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int _ = 1;
    cin >> _;
    //	cout<<fixed<<setprecision(1);
    while (_--) {
        solve();
    }
    return 0;
}

```

## 自我分析

这道题想到了先求出整理的异或和sum，然后构造一个异或的序列，使得结果为A，转换成了求sum^A+A的最大值。然后因为是两个变量，所以就卡在这里了，后来才查到用到$a + b = (a \oplus b) + 2\cdot(a \mathrel{\&} b)$这个公式转化成求sum+(~sum&A).运用线性基来求出这个整体的最大值，线性基不熟悉，然后异或公式的推导也有问题。这两个点得强加复习

# 最大公约数图（GCD Graph）

## 题目描述

输入模式: 标准输入  
输出模式: 标准输出  
时间限制: 2 秒  
空间限制: 256 MB  

小羊得到了一个有向图，其顶点由正整数 1,2,... 命名，共有无穷多个顶点。

对于每对满足 $1 \le i < j$ 的整数 $(i,j)$，存在一条从 $i$ 到 $j$、权值为 $w_{i,j} = \gcd(i,j)$ 的有向边，即若你决定从 $i$ 走到 $j$，需要支付代价 $w_{i,j}$。图中不存在其他边。

对于每对满足 $1 \le u < v$ 的整数 $(u,v)$，定义 $\operatorname{cost}(u,v)$ 为从 $u$ 到 $v$ 的最小总代价。

小羊想知道对于某些 $(l,r,n)$，$\sum_{i=l}^r \operatorname{cost}(i, n)$ 的值是多少，你能告诉他答案吗？

注意，$\gcd(x,y)$ 表示 $x$ 与 $y$ 的最大公因数。

### 输入格式

每个测试包含多组测试用例。第一行包含测试用例数 $T$（$1 \le T \le 100$）。接下来是每组测试用例的描述。

每组测试用例仅一行，包含三个整数 $l, r, n$（$1 \le l \le r < n \le 10^7$）——小羊询问的参数。

注意对 $\sum n$ 没有限制。

### 输出格式

对于每组测试用例，输出一个整数，表示 $\sum_{i=l}^r \operatorname{cost}(i, n)$ 的值。

#### **样例输入**

```c++
3
1 5 6
2 33 36
1 99 100
```

#### **样例输出**

```c++
8
54
158
```

## 题解

如果存在一个质数 $p$ 在 $u,v$ ($u<v$) 之间，则 $\operatorname{dis}(u,v)$ 不超过 $u\to p$ 的边权加上 $p\to v$ 的边权，因此答案不超过 $2$。  
在 $10^7$ 的范围内，质数分布是密集的，其中最远的距离是 $150$，所以我们可以先找到不超过 $r$ 的最大质数 $p$。  

对于 $[l,r]$ 中小于 $p$ 的部分，$\operatorname{dis}(x,n)$ 不超过 $2$，而 $\operatorname{dis}(x,n)$ 等于 $1$ 当且仅当 $x$ 和 $n$ 互质，所以我们只需求出这个范围内和 $n$ 互质的数的个数即可。可以枚举 $n$ 的因子进行容斥。  

而对于剩余的部分，由于长度很小，我们直接使用 DP。记 $\operatorname{dp}[x]$ 为从 $x$ 出发到 $n$ 的最短路长度。我们从大到小从 $n-1$ 遍历到 $p$，转移方程  
$$ \operatorname{dp}[u] = \min_{i=u+1}^{n} \bigl(\operatorname{dp}[i] + \gcd(i,u)\bigr), $$  
边界 $\operatorname{dp}[n] = 0$。  

时间复杂度为 $O\bigl(2^{\operatorname{prime\_factor\_count}(n)} + \operatorname{prime\_gap}^2 \log n\bigr)$，其中 $\operatorname{prime\_factor\_count}(n)$ 是 $n$ 的质因子个数，$\operatorname{prime\_gap}$ 表示 $r$ 到小于它的最大质数的距离。

```c++
#include<bits/stdc++.h>
#define int long long
#define endl '\n'
using namespace std;
const int N = 1e7 + 10;
const int MOD = 1;
#define MOD debug;
int prim[N], tot;
int bj[N]; //用来记录每个数的最小质因子
void getprim() {
    for (int i = 2; i < N; i++) {
        if (!bj[i]) {
            prim[++tot] = i;
            bj[i] = i; //每个数字除以这个数字
        }
        for (int j = 1; j <= tot && prim[j] * i < N; j++) {
            bj[prim[j] * i] = prim[j];
            if (i % prim[j] == 0)break;
        }
    }
}

int cal(int a,int b) {
    return a / b;
}

void solve() {
    int l, r, n;
    cin >> l >> r >> n;
    int ls = n;
    vector<int> p;
    for (int i = 2; i * i <= ls; i++) {
        if (ls % i == 0) {
            p.push_back(i);
            while (ls % i == 0) {
                ls /= i;
            }
        }
    }
    if (ls > 1) {
        p.push_back(ls);
    }
    auto js = [&](int a,int b) {
        int cnt = 0;
        for (int zt = 1; zt < (1 << p.size()); zt++) {
            int sz = __builtin_popcount(zt);
            int ls = 1;
            for (int i = 0; i < p.size(); i++) {
                if ((zt >> i) & 1) {
                    ls *= p[i];
                }
            }
            if (sz & 1) {
                cnt += cal(b, ls) - cal(a - 1, ls);
            } else {
                cnt -= cal(b, ls) - cal(a - 1, ls);
            }
        }
        return cnt;
    };
    int ll = 0, rr = tot + 1;
    while (ll + 1 < rr) {
        int mid = ll + rr >> 1;
        if (prim[mid] <= n) {
            ll = mid;
        } else {
            rr = mid;
        }
    }
    int wz = prim[ll];
    int ss = js(l, min(wz, r));
    int ans = 2 * ss + min(wz, r) - l + 1 - ss;
    for (int i = wz + 1; i <= r; i++) {
        int bj = 0;
        if (__gcd(i, n) == 1) {
            ans++;
            continue;
        }
        for (int j = i + 1; j < n; j++) {
            if (__gcd(i, j) == 1 && __gcd(j, n) == 1) {
                ans += 2;
                bj = 1;
                break;
            }
        }
        if (!bj) {
            ans += 3;
        }
    }
    cout << ans << endl;
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int _ = 1;
    cin >> _;
    getprim();
    //	cout<<fixed<<setprecision(1);
    while (_--) {
        solve();
    }
    return 0;
}

```

## 自我分析

这道题想了差不多一个小时没有想出来，然后后面我队友想出来了，对于这种大规模用算法，小规模用暴力的分类讨论方法我不是很擅长，每次遇到这种根据数据规模来适当使用暴力的算法，我基本上就很难想出来。我后面没有用dp的方法，我感觉最长长度不会超过3，所以我排除长度为1，长度为2的之后，我直接令其为三，便通过了

# 懒得打乱（Lazy Shuffling）

## 题目描述

小羊被分配了一个打乱长度为 $n$ 的排列的任务。一个排列就是一个长度为 $n$ 的包含 $1,2,\dots,n$ 中的数各一次的序列。

小羊的领导会通过比较打乱前后数组的逆序对数量来评价小羊的工作。  
我们定义一个排列 $A = [A_1, A_2, \dots, A_n]$ 的逆序对数量为  

$$
f(A) = \sum_{1 \le i < j \le n} [A_i > A_j]
$$

其中 $[P]$ 是指示函数：当条件 $P$ 成立时值为 $1$，否则为 $0$。  

设打乱前后的排列分别是 $A$ 和 $A'$，那么领导会计算  

$$
g(A, A') = |f(A) - f(A')|
$$

来检查小羊的工作。领导希望这个数值越大越好。

但小羊是个懒人。他不想真的好好打乱数组，所以他决定使用一个固定的长度为 $n$ 的排列 $p = [p_1, p_2, \dots, p_n]$，当他工作中遇到一个排列 $A = [A_1, A_2, \dots, A_n]$ 的时候，他就可以提交  

$$
f_p(A) = [A_{p_1}, A_{p_2}, \dots, A_{p_n}]
$$

作为他的工作成果。可以证明 $f_p(A)$ 也是一个排列。

小羊认为一个排列是**幸运的**，当且仅当使用上述打乱方法时，这个排列 $A$ 能最大化 $g(A, f_p(A))$。也就是说，对于任意一个长度为 $n$ 的排列 $A_0$，都有  

$$
g(A, f_p(A)) \ge g(A_0, f_p(A_0))
$$

那么有多少个幸运的排列呢？鉴于答案可能很大，请将结果对 $998244353$ 取模后输出。

### 输入格式

输入包含两行。

第一行包含一个整数 $n\ (1 \le n \le 22)$ —— 排列的长度。  
第二行包含 $n$ 个整数 $p_1, p_2, \dots, p_n\ (1 \le p_i \le n)$ —— 小羊用于打乱 $A$ 的排列 $p$。保证对于所有 $1 \le i < j \le n$，$p_i \ne p_j$（即 $p$ 是一个排列）。

### 输出格式

输出一个整数，表示幸运的排列的数量对 $998244353$ 取模的结果。

#### 样例输入

```c++
3
3 1 2
```

#### 样例输出

```c++
4
```

## 题解

对于一组 $1\le i<j\le n$，考虑一组 $A_{p_i},A_{p_j}$ 是如何影响打乱后的逆序对数量的。  
如果 $p_i<p_j$ ，则打乱前后这两者要么都形成逆序对，要么都不形成逆序对，所以不影响前后逆序对之差。  
如果 $p_i>p_j$ ，则如果 $A_{p_i}>A_{p_j}$ ，则对打乱后逆序对变化的贡献是 $+1$，因为一开始是逆序对，后来不是了；否则对打乱后逆序对变化的贡献是 $-1$ 。  
所以只需考虑 $p$ 的逆序对，打乱前后逆序对的差的绝对值就是这些贡献和的绝对值。

因为这些贡献绝对值都是 $1$，能否让这些逆序对对结果的贡献都是同方向的，即都是 $+1$ 或 $-1$，让整体变动的绝对值最大化呢？  
答案是肯定的，$A=[1,2,\dots,n]$ 正是这样的一个排列。  
所以我们要求的就是对于 $p$ 中所有的逆序对 $(i,j)\ (1\le i<j\le n)$，要么都有 $A_i<A_j$ ，要么都有 $A_i>A_j$ 的排列 $A$ 的个数。  
根据这些大小关系，可以建立一个从小的数指向大的数的一个有向图，我们相当于要求其拓扑序个数。

考虑一个还没确定拓扑序的子集 $S$ 对应的答案是 $dp[S]$，我们可以任意去除一个子集中对它没有入边的点 $u$，则我们可以将 $dp[S-\{u\}]$ 转移到 $dp[S]$ 中去。  
这里可以直接枚举 $u$，而由于 $n$ 数据范围很小，可以用 $[0,2^n)$ 的一个数表示一个点的入边情况是否有入边，将其和 $S$ 进行与运算看结果是否为 $0$ 即可 $O(1)$ 快速判断。

注意到 $A_i<A_j$ 和 $A_i>A_j$ 对应的图恰好相反，因此一个图的拓扑序反过来就是另一个图的拓扑序，因此有一一对应关系，只需求其中一个图就行。  
有重复计算吗？如果存在逆序对，则两个条件都满足就直接成环了。所以只有没有逆序对的时候会重复计算。没有逆序对时，$A$ 数组永不改变，因此每个排列都满足要求，直接输出 $n!$。  
时间复杂度为 $O(n 2^n)$ 。

```c++
#include <bits/stdc++.h>
using namespace std;

const int MOD = 998244353;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    vector<int> p(n);
    for (int i = 0; i < n; ++i) {
        cin >> p[i];
        // p[i] 是 1..n 的值，我们保留原值，因为只比较大小，不影响
    }

    // 检查 p 是否有逆序对
    bool has_inv = false;
    for (int i = 0; i < n && !has_inv; ++i)
        for (int j = i + 1; j < n && !has_inv; ++j)
            if (p[i] > p[j]) has_inv = true;

    // 没有逆序对，答案就是 n!
    if (!has_inv) {
        long long fact = 1;
        for (int i = 2; i <= n; ++i)
            fact = fact * i % MOD;
        cout << fact << '\n';
        return 0;
    }

    // 建图：全负情况，逆序对 (i,j) 加边 i -> j
    vector<int> in_mask(n, 0);
    for (int i = 0; i < n; ++i)
        for (int j = i + 1; j < n; ++j)
            if (p[i] > p[j])
                in_mask[j] |= (1 << i);

    // 状压 DP
    const int FULL = (1 << n) - 1;
    vector<long long> dp(1 << n, 0);
    dp[0] = 1;

    for (int mask = 1; mask <= FULL; ++mask) {
        long long sum = 0;
        // 遍历 mask 中的每一个位（lowbit 加速）
        int m = mask;
        while (m) {
            int u = __builtin_ctz(m);   // 最低位 1 的位置
            m &= m - 1;                 // 去掉最低位
            if ((in_mask[u] & mask) == 0) {
                sum += dp[mask ^ (1 << u)];
            }
        }
        dp[mask] = sum % MOD;
    }

    long long ans = (2 * dp[FULL]) % MOD;
    cout << ans << '\n';

    return 0;
}
```

## 自我分析

这种题根本不会写，以前遇到这种题没写，现在看题解基本上也看不懂。估计以后做这种题还是会错。题目大概是只有p的逆序对才会对答案统计+1或者-1，因为外部有绝对值，所以需要整体确保符号统一，根据逆序对建图，然后跑二进制枚举dp。

# 神奇的树（Fabulous Tree）

## 题目描述

大小为 $n$ 的树是一个有 $n-1$ 条双向边的连通图。**带标签的树**是指每个顶点和每条边都被赋予一个非负整数值的树。  

顶点 $u$ 被赋予的值为 $a_u$，$u$ 与 $v$ 之间的边被赋予的值为 $w_{u,v}$。若对于每对存在边的 $(u,v)$，均满足 $w_{u,v} = |a_u - a_v|$，则称该带标签的树为**神奇的树**。

对于一棵树，其**复杂度**定义为所有顶点被赋予的值中最大值与最小值之差。

给定一棵以 $1$ 为根的带标签树，但各顶点的值缺失。对于从 $1$ 到 $n$ 的每个 $i$，若将顶点 $i$ 对应的子树的顶点赋值，使其成为一棵神奇的树，该子树的最小复杂度是多少？注意各子树的问题需独立求解。即某子树复杂度的最小化并不意味着其他子树的复杂度也一定达到最小。

顶点 $u$ 的子树包含且仅包含所有满足“从 $1$ 到 $v$ 的最短路径经过顶点 $u$”的顶点 $v$，并保留原树的连接关系。

### 输入格式

每个测试包含多组测试用例。第一行包含测试用例数 $T$（$1 \leqslant T \leqslant 10^4$）。接下来是每组测试用例的描述。

每组测试用例的第一行包含一个整数 $n$（$3 \leqslant n \leqslant 10^5$）——树的顶点数。

接下来 $n-1$ 行，第 $i$ 行包含三个整数 $u_i, v_i$ 和 $w_{u_i,v_i}$  
（$1 \leqslant u_i, v_i \leqslant n$, $u_i \neq v_i$, $0 \leqslant w_{u_i,v_i} \leqslant 5000$）  
——树中第 $i$ 条边的两个端点及其被赋予的值。保证给定的边构成一棵树。

记 $W$ 为同一测试中所有测试用例的 $w_{u_i,v_i}$ 最大值。保证所有测试用例的 $n$ 之和不超过 $10^5$。

### 输出格式

对于每组测试用例，输出 $n$ 个整数，分别表示顶点 $1,2,\dots,n$ 各自子树的最小复杂度。

#### 样例输入

```c++
3
3
1 2 1
2 3 1
4
1 2 2
2 3 1
3 4 2
5
1 2 4
1 3 1
2 4 2
2 5 1
```

#### 样例输出

```c++
1 1 0
3 2 2 0
4 2 0 0 0
```

## 题解

首先，对于一棵树，设其最大边权为 $W$，则该树点权极差的最小值不超过 $2W-1$。  
证明：直接对根节点赋值 $X$，然后从根出发进行搜索，每个新搜索到的点一定能在区间 $[X-W,\ X+W)$ 内找到一个可行的权值。

本题需要使用 DP。设计如下：  
记 $dp[u][i]$ 表示以 $u$ 为根的子树，在**根节点点权为 $V$，且子树的最小值不小于 $V-i$** 的前提下，子树最大值减去 $V$ 的最小值。  
那么 $u$ 对应子树的答案即为 $\min_i (dp[u][i] + i)$。这里只需考虑 $i \le 2W-1$。

考虑 $u$ 的子结点 $v$，两点间边权为 $w$。由 $v$ 的子树会生成对 $dp[u]$ 的更新，记合并过程中的临时数组为 $tmp$。

对于 $v$ 的某个状态 $i$：

- 若 $v$ 的点权比 $u$ 小 $w$，则 $v$ 子树的最小值比 $u$ 的点权 $V$ 小 $i+w$。  
  因此对于所有 $i' \in [i+w,\ 2W-1]$，可以用 $\max(0,\ dp[v][i] - w)$ 更新 $tmp[i']$。

- 若 $v$ 的点权比 $u$ 大 $w$，则 $v$ 子树的最小值比 $V$ 小 $i-w$。  
  因此对于所有 $i' \in [\max(0,i-w),\ 2W-1]$，可以用 $dp[v][i] + w$ 更新 $tmp[i']$。

对每个子结点 $v$ 做上述更新后，将 $tmp$ 合并到 $dp[u]$ 上：
$$dp[u][i] = \max(dp[u][i],\ tmp[i])$$

上述转移均是对一段后缀取 $\max$，无需使用带 $\log$ 的数据结构。  
对于每轮合并，先准备一个数组，在每个后缀的左端点记录对应的更新值，然后从左到右扫描，维护前缀最大值，即可 $O(2W)$ 得到整个 $tmp$ 数组。

另外，可以利用 $dp[u][i]$ 关于 $i$ 的单调性，对每个 $i$ 直接找到两种转移的最优起始位置并提取结果取 $\max$。

总时间复杂度为 $O(nW)$。

```c++

```

## 自我分析

这道题依旧还是不会，就算这个退化成串，在串上跑我也不会。赛后补题补出来的
